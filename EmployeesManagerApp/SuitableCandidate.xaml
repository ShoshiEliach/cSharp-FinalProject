<Window x:Class="UI.SuitableCandidate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="SuitableCandidate" Height="450" Width="800">
    <Grid>
        <!-- Define rows and columns for layout -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ComboBox at the top -->
        <ComboBox x:Name="myComboBox"
                  Grid.Row="0"
                  Margin="10"
                  Width="200"
                  VerticalAlignment="Top"
                  HorizontalAlignment="Left"
                  ItemsSource="{Binding ComboBoxItems}"
                  SelectedItem="{Binding SelectedItem}" SelectionChanged="myComboBox_SelectionChanged"/>

        <!-- DataGrid below the ComboBox -->
        <DataGrid x:Name="myDataGrid"
                  Grid.Row="1"
                  Margin="10"
                  AutoGenerateColumns="True"
                  ItemsSource="{Binding DataGridItems}"
                  CanUserAddRows="False"/>
    </Grid>
</Window>
